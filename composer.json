{
    "require-dev": {
        "phpunit/phpunit": "^9.5",
        "squizlabs/php_codesniffer": "^3.7"
    },
    "scripts": {
        "test": "phpunit --colors=always",
        "lint": "phpcs --standard=PSR12 src/ tests/"
    }
}
